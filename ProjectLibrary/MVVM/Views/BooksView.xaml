<UserControl
	x:Class="ProjectLibrary.MVVM.Views.BooksView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:ProjectLibrary.MVVM.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="450"
	d:DesignWidth="800"
	mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="50" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Grid>

			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<TextBox Margin="10" />
			<ComboBox Grid.Column="1" Margin="10" />
			<ComboBox Grid.Column="2" Margin="10" />
			<Button
				Grid.Column="3"
				Margin="10"
				Command="{Binding AddBookCommand}"
				Content="Добавить" />
		</Grid>
		<Grid Grid.Row="1">
			<ListView ItemsSource="{Binding Books}" Style="{StaticResource ListStyle}" />
		</Grid>
	</Grid>
</UserControl>
